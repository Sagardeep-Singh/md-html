version: "3.7"

services:
  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: 'app_db'
      MYSQL_USER: 'app_user'
      MYSQL_PASSWORD: "ifCASfeGS9x3owm8"
      MYSQL_ROOT_PASSWORD: "ifCASfeGS9x3owm8"
    ports:
      - '3306:3306'
    volumes:
      - my-dev-db:/var/lib/mysql
  python:
    image: api
    restart: always
    build:
      context: .
      dockerfile: ./DockerFiles/python.Dockerfile
    depends_on: 
      - db
      - js
    ports:
      - 80:8000
    volumes:
      - ./backend:/app/
      - app-documents-dev:/documents
      - app-static-dev:/app/build
    environment: 
      DOCUMENT_PATH: '/documents/'
  js:
    image: node:16.6.1-buster
    volumes:
      - app-static-dev:/build
      - ./frontend:/app
    entrypoint: bash /app/start.sh
      
# Names our volume
volumes:
  my-dev-db:
  app-static-dev:
  app-documents-dev: